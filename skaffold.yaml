apiVersion: skaffold/v2beta29
kind: Config
metadata:
  name: nie-appt-alerter
build:
  artifacts:
  - image: cdutten/nie-appt-alert
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - k8s/nie-app-alerter-cj.yaml
    - k8s/nie-appt-alerter-ns.yaml
